# Byte-compiled / cached
__pycache__/
*.py[cod]

# Build artifacts / packaging
build/
dist/
*.egg-info/

# Virtual environments
venv/
.venv/
env/

# Runtime logs (rotated by the app's logger — regenerated every run)
logs/*.log
logs/*.log.*

# AI Stage 6 (README §53) self-play corpora — generated data, reproducible
# from the base seed with `python -m game.selfplay`.
corpus/
corpus-*/
